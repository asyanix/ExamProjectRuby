<%= form_with(model: platform, class: 'form_new') do |form|%>
  <% if platform.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(platform.errors.count, "error") %> prohibited this platform from being saved:</h2>

      <ul>
        <% platform.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :releaseDate, style: "display: block" %>
    <%= form.date_field :releaseDate %>
  </div>

  <div>
    <%= form.label :manufacturer, style: "display: block" %>
    <%= form.text_field :manufacturer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

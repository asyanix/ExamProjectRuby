<h1>Platforms</h1>

<ul>
  <% @platforms.each do |platform| %>
    <li>
      <%= link_to platform.name, platform_path(platform) %>
    </li>
  <% end %>
</ul>

<%= button_to "Create New Platfotm", new_platform_path, method: :get %>

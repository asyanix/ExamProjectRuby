<p style="color: green"><%= notice %></p>

<%= render @game %>

<div>
  <%= button_to 'Edit', edit_game_path(@game), method: :get, class: 'btn-edit' %>
  <%= button_to 'Delete', game_path(@game), method: :delete, class: 'btn-danger' %> 
  <%= button_to 'Back to games', games_path, method: :get, class: 'btn' %>
</div>

<h1>Games</h1>

<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to game.title, game_path(game) %>
    </li>
  <% end %>
</ul>

<%= button_to 'Create New Game', new_game_path, method: :get %>
